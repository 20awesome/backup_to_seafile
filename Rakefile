require "bundler/gem_tasks"
#require "rspec/core/rake_task"
require 'backup_to_seafile'

#spec = Gem::Specification.find_by_name 'backup_to_seafile'
#load "#{spec.gem_dir}/lib/create_config.rake"
#RSpec::Core::RakeTask.new(:spec)

task :default => :spec
import "./lib/create_config.rake"
